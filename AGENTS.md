# Study 1 analysis agent entry point

Before changing the paper pipeline, read `FABLE_HANDOFF.md` at the repository root.

The active analysis branch is `study1-analysis`. Preserve all existing untracked
Phase 1 literature files. Do not restart the paper pipeline, regenerate the
statistical analysis, merge another branch, or begin prose drafting until the
checkpoint described in `FABLE_HANDOFF.md` has been resolved with the user.

